/*
    Standardized code to display all details of a given report number and 
    of the mobile user who sent it in. Modal; activated through either 
    dash.html or reports.html. 

    Uses reportTemplate and fills with information recieved from API and returns the filled in report modal. 
    Takes in a reportID.
*/

var template = require("reportTemplate.html");

function generateReport(reportID) {
    return template;
}
